# 腾讯实习经历

### 1.项目介绍：

我参与的项目是 **安御平台**的安全风控评估服务，为微众银行等金融机构提供反欺诈能力。

它是作为微众客户的专属接入服务：银行通过微众专线把请求送进来，

服务根据请求类型，聚合多个下游风控数据源做综合风险评估，

最终返回加密后的风险标签结果，供银行做通过、拦截或转人工审核的决策。

```
银行提交申请/交易信息
          ↓
风控网关解密并标准化手机号
          ↓
将手机号转换为下游约定的哈希标识
          ↓
并发查询数据源A、数据源B、数据源C
          ↓
获得风险标签、风险特征或风险分
          ↓
结合银行本次请求信息进行规则判断
          ↓
向银行返回最终风险结果
```

## 2.工作点：

### 工作点一：

我第一个大的工作，是围绕微众银行这条反欺诈链路做建设。从接入数据源，到让它能稳定上线、可观测，算是一套完整的东西。

首先是接入新的数据源。当时银行请求打到我们这边以后，里面会有手机号、业务场景这些信息。我们原来已经会查一些风险服务，后面业务觉得现有信息不够，希望再补两个数据源，拿到更多风险标签或者评分，帮助做更准确的判断。

我做的第一步，是把银行传过来的敏感字段处理一下。像手机号，银行传过来是加密 的，我们不能直接拿着密文查下游，所以会先在服务调 sdk 内部解密、做格式处理，然后再按下游要求调用公共安全组件 sdk 转成哈希值。下游拿到的是哈希后的标识，不是明文。

然后我把这几个数据源改成并发查询。因为如果一个一个查，整体耗时会叠加，风控接口又比较看重响应时间。并发之后，整体时间主要看最慢的那个数据源。

数据回来之后，每个数据源的字段和返回格式不一样，我这边做统一解析，转成内部能识别的风险结果，再按照现有规则去做评分和聚合，最后组装成银行需要的格式返回。

链路做完之后，我又补了一套可观测性监控。之前线上出现接口超时或者调用失败时，主要依赖日志排查。虽然能查到单次请求的错误，但很难快速看出是哪个客户、哪个接口或者哪个地域出现了问题，也不容易判断耗时是在我们自己的主流程，还是某个下游数据源。

所以我给每个请求维护了一份监控上下文。请求进来后，先记录客户标识、接口类型、服务地域、可用区和部署版本；请求结束时，再记录整个接口的总耗时以及成功、失败原因。

对于请求过程中调用的多个下游数据源，我也分别记录了每次调用的数据源类型、调用耗时和调用结果。如果下游调用发生重试，每次实际调用都会单独记录，这样可以看到首次调用和重试分别花了多长时间、在哪一次发生了失败。

因为多个下游数据源是并发查询的，多个协程可能同时写入监控数据，所以我对监控记录加了互斥锁，避免并发写入导致数据竞争。

最后，我在请求入口通过 `defer` 做统一收口。无论请求正常返回、参数解析失败、接口不支持，还是处理过程中发生 panic，都会在请求结束时计算整体耗时，并把主请求指标和已经记录的下游调用指标统一上报。

这样监控平台就可以按照客户、接口、地域和下游类型等维度，实时查看调用量、平均耗时和失败率。线上出现问题后，就可以快速判断影响了哪些客户和接口，再进一步定位是主服务的问题，还是某个下游数据源出现了异常。

#### Q1. 多个下游并发查，结果类型还不一样，为什么用 `WaitGroup`，不用 channel 或 `errgroup`？

**你的回答：**

我那两个下游返回的东西类型不一样，一个大概是风险分，一个是风险标签字符串。如果用 channel，要么我得定义一个统一的结果结构体去包住不同字段，要么每个下游单独一个 channel，代码反而啰嗦。而且我这个场景是"发起后等它们全部完成，再统一解析"，没有"边查边处理"或者"按完成顺序消费结果"的需求，所以 `WaitGroup` 最直接。

`errgroup` 我也知道，它好在一个 goroutine 报错能把错误带出来、还能配合 context 做取消。但我这两个下游之间没有强依赖，也没有"一个挂了必须立刻取消另一个"这种诉求——每个下游自己失败就走它自己的兜底默认值，互不影响。所以这里 `WaitGroup` 足够，用 `errgroup` 属于过度设计。

**追问：如果其中一个下游 panic 或返回错误，你怎么把"部分失败"带回主流程？共享变量被并发写怎么保证安全？**

**你的回答：**

我其实不是用一堆共享变量乱写，而是先构造一个结果结构体，把每个下游该填的字段提前定义好，初始给上默认值。每个 goroutine 只写自己负责的那一个字段——比如数据源 A 只写 A 的字段，数据源 B 只写 B 的字段。因为它们是不同的内存地址，互相不重叠，所以这块 **本身不需要加锁** 。

真正需要加锁的是另外一件事：我在每个下游调用时会把这次调用的类型、耗时、结果往一个共享的监控 slice 里 `append`。多个 goroutine 同时 `append` 同一个 slice，才会有数据竞争，所以那里我加了 `sync.Mutex`。结果字段是各写各的，监控记录是共享 append，两者要分开看。

---

#### Q2. 说说 `WaitGroup` 最容易踩的坑。

**你的回答：**

两个最经典的坑：

第一，`Add` 不能放进 goroutine 里。因为 goroutine 的调度是不确定的，可能主 goroutine 已经跑到 `Wait()` 了，子 goroutine 里的 `Add` 还没执行，那 `Wait` 发现计数器是 0 就直接返回了，任务根本没做完。

第二，`Done` 和 `Add` 的数量必须对得上。`Done` 调多了，计数器变负数，直接 panic。

**追问：你代码里用 `recover` 兜 panic，那 `Done()` 放哪了？为什么这么放才安全？**

**你的回答：**

`Done()` 我放在 [`defer`](command:gongfeng.gongfeng-copilot.chat.open-symbol-in-file?%5B%7B%22%24mid%22%3A1%2C%22fsPath%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22external%22%3A%22file%3A%2F%2F%2FUsers%2Fzhouzehua%2Frating%2F%25E5%25AE%259E%25E4%25B9%25A0%25E7%25BB%258F%25E5%258E%2586.md%22%2C%22path%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22scheme%22%3A%22file%22%7D%2C%22defer%22%2C%5B%7B%22line%22%3A50%2C%22character%22%3A13%7D%2C%7B%22line%22%3A50%2C%22character%22%3A18%7D%5D%5D) 里，而且确保它一定执行。因为如果 goroutine 里业务 panic 了，被 `recover` 兜住以后，代码会直接往下走或者 return，如果没有 [`defer`](command:gongfeng.gongfeng-copilot.chat.open-symbol-in-file?%5B%7B%22%24mid%22%3A1%2C%22fsPath%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22external%22%3A%22file%3A%2F%2F%2FUsers%2Fzhouzehua%2Frating%2F%25E5%25AE%259E%25E4%25B9%25A0%25E7%25BB%258F%25E5%258E%2586.md%22%2C%22path%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22scheme%22%3A%22file%22%7D%2C%22defer%22%2C%5B%7B%22line%22%3A50%2C%22character%22%3A13%7D%2C%7B%22line%22%3A50%2C%22character%22%3A18%7D%5D%5D) 保证 `Done()`，这个 goroutine 就没把计数器减一，主 goroutine 的 `Wait()` 会永远阻塞住。所以 panic 处理和 `Done()` 必须都靠 [`defer`](command:gongfeng.gongfeng-copilot.chat.open-symbol-in-file?%5B%7B%22%24mid%22%3A1%2C%22fsPath%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22external%22%3A%22file%3A%2F%2F%2FUsers%2Fzhouzehua%2Frating%2F%25E5%25AE%259E%25E4%25B9%25A0%25E7%25BB%258F%25E5%258E%2586.md%22%2C%22path%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22scheme%22%3A%22file%22%7D%2C%22defer%22%2C%5B%7B%22line%22%3A50%2C%22character%22%3A13%7D%2C%7B%22line%22%3A50%2C%22character%22%3A18%7D%5D%5D) 兜底，顺序上就是：`defer wg.Done()` 先注册，然后里面再处理业务、再 `recover`。

---

#### Q3. 你为什么要加锁，而不是让每个下游把结果通过 channel 发给一个单独的 goroutine 串行写入？

**你的回答：**

channel 串行写入要额外起一个常驻的消费 goroutine，还要管它的生命周期和退出，增加了复杂度。而且监控写入是很高频的操作，走 channel 会引入额外的数据拷贝和 goroutine 调度开销。我这个场景只是往一个 slice 里 `append` 记录，临界区非常小，用一把 `Mutex` 锁一下 `append` 就够了，实现最简单、开销也最低。

channel 更适合那种"生产和消费速率不匹配、需要解耦和缓冲"的场景，比如批量异步上报。当时还没到那一步，所以先不加。

**追问：高并发下，加锁和 channel 串行哪个更好？你当时怎么权衡？**

**你的回答：**

两者都有同步成本。锁的代价是竞争时 goroutine 阻塞或自旋；channel 的代价是缓冲、拷贝和调度。关键不在选哪个，而在 **临界区要尽量小** 。我锁住的只有 `append` 这一下，不锁 IO、不锁计算，所以锁竞争很轻。

而且说实话，我这里的"多协程"其实就两三个下游 goroutine，并发度很低，锁竞争根本不激烈，所以选 `Mutex` 是最省事的。如果未来下游数量变大、写入变多，我才会考虑换 channel 或者批量聚合的方案。

---

#### Q4. 你加的是 `sync.Mutex`，为什么不用 `sync.RWMutex`？你的监控是读多写少还是写多读少？

**你的回答：**

监控记录是典型的 **写多读少** 。每个请求都要往里写下游调用记录，而"读"基本只在请求结束时统一上报那一下才发生，频率低得多。`RWMutex` 的优势是允许并发读，适合读多写少的场景；在写多的情况下，它还要额外维护读锁计数，开销反而比 `Mutex` 更大，所以这里用 `Mutex` 更合适。

**追问：高频写、低频读加互斥锁，会不会成为性能瓶颈，反过来拖慢主链路？**

**你的回答：**

关键是控制临界区。我锁里只做 `append`，不做任何耗时操作，所以单个请求持锁时间极短。监控上报本身又是旁路逻辑，就算有轻微竞争，也不会阻塞主业务太久。我在目标 QPS 下实际验证过，这里不是瓶颈。

如果真到了锁竞争严重的那一步，正确的方向不是继续优化锁，而是像下一题说的那样，改成"每个协程写自己的私有 buffer，最后再合并"，把共享写入彻底消掉。

---

#### Q5. 有没有考虑过无锁方式，比如每个 goroutine 写自己的私有 buffer 最后再合并？

**你的回答：**

考虑过。每协程私有 buffer 确实能完全避免锁，但它的问题是：内存占用会翻倍，每个协程都要维护一份，最后还要做 merge，代码复杂不少。而全局加锁实现简单，临界区又小。

再结合我实际的并发度——就两三个下游协程在写，锁竞争几乎可以忽略——所以没必要上无锁方案，用锁是最务实的选择。如果将来下游数量涨到几十上百，我才会重新评估"私有 buffer 合并"或者"单协程串行消费"这两种方案。

---

#### Q6. 你按"客户 + 接口 + 地域 + 可用区 + 版本"打指标，知道维度会指数级膨胀吗？

**你的回答：**

知道，这就是高基数（high cardinality）问题。维度组合是笛卡尔积，一旦某个维度的取值特别多，时序数据量会爆炸，查询也会变慢。这里面最危险的就是"客户标识"这种高基数字段——客户数量多了以后，直接把它当标签打进去，监控系统的存储和查询成本都会扛不住。

所以我当时在设计维度时有意识地做了控制：不会把手机号、用户 ID 这类高基数字段直接当标签，客户维度也不会无限细化，只保留必要的、取值空间可控的维度。高基数字段要么聚合、要么单独处理，不打进核心指标标签里。

**追问：监控是同步还是异步上报？上报失败或监控系统挂了，会影响业务主流程吗？**

**你的回答：**

不是每次下游调用都实时打点，而是在请求结束时，通过 [`defer`](command:gongfeng.gongfeng-copilot.chat.open-symbol-in-file?%5B%7B%22%24mid%22%3A1%2C%22fsPath%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22external%22%3A%22file%3A%2F%2F%2FUsers%2Fzhouzehua%2Frating%2F%25E5%25AE%259E%25E4%25B9%25A0%25E7%25BB%258F%25E5%258E%2586.md%22%2C%22path%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22scheme%22%3A%22file%22%7D%2C%22defer%22%2C%5B%7B%22line%22%3A50%2C%22character%22%3A13%7D%2C%7B%22line%22%3A50%2C%22character%22%3A18%7D%5D%5D) 统一收口、统一上报。相当于每个请求聚合一次，而不是每个下游调用发一次，这样既减少上报频率，也避免监控拖慢主链路。

监控对业务来说是 **弱依赖** ，上报失败或监控系统挂了，只会导致指标丢失，不会影响这笔风控请求的返回。我始终把它放在旁路，不阻塞主流程。

---

#### Q7.这些下游调用的监控，每次重试是单独记录还是覆盖？

**你的回答：**

单独记录。因为如果首次调用失败了、重试成功了，只看最终结果会掩盖真实耗时。我把首次调用和每次重试都单独记一条，这样能清楚看到"首次花了多久、在哪一次失败、重试又花了多久"，定位问题更准。这也解释了为什么会有多条下游记录并发写入，所以需要加锁。

---

#### 工作点二：

第二个工作是容灾这块，我给这条风控链路做了主被动两级降级，保证任何一个数据源出问题都不会拖垮整笔请求。

风控接口是银行同步调用的，对可用性要求比较高，但下游数据源偶尔会有超时、报错或者维护的情况。所以我先做了一层被动降级，也就是异常兜底：正常情况下先调用下游，如果某个数据源超时、调用报错，或者重试次数多了之后还是失败，我们就给这个数据源构造一个默认结果，让其他数据源继续聚合。这样某一个源挂了，不会把整笔请求搞失败，其他正常的数据还是会继续返回。

但是后来发现，被动降级只能解决“请求不要失败”的问题，它还是会实际调用下游。比如我们已经知道某个数据源正在维护，这段时间内每个请求还是会先调用它，然后等待超时或者重试失败，最后才走兜底。这样不仅会增加每次请求的耗时，也会占用线程和连接资源，而且还会继续给故障中的下游增加压力。

所以后面我又增加了一个主动降级开关。这个开关是按数据源配置的，放在动态配置中心里。请求进来以后，在调用某个数据源之前，先判断它的开关状态。如果这个数据源处于维护或者已知不可用状态，我们就把开关关闭，直接跳过这次下游调用，构造该数据源对应的默认结果，参与后面的聚合。

要让这个开关真正好用，关键在配置热更新。我们原来服务在服务启动时从配置中心拉取一次配置，后面配置发生变化，服务感知不到，必须重启才能生效。而降级场景恰恰是最不能重启的，因为需要的是问题发生时立刻切开关止损。

所以我做了这么一套热更新：服务启动时先通过 Get 拉取初始配置，保证启动后有一个明确状态；然后启动一个 Watch goroutine 持续监听配置变化。这样运维在配置中心修改开关后，服务不用重启就能动态生效。

热更新时我比较关注配置一致性。比如单独一个降级开关字段可以用 atomic.Bool 更新；如果后面把超时时间、重试次数、降级模板这些关联配置也放进来，就不能一个字段一个字段地修改，否则请求可能读到一半新、一半旧的配置。

所以我的做法是先解析并校验出一份完整的新配置，再通过 atomic.Value 一次性替换。每个请求只读取一次配置快照，整个处理过程都使用同一个版本。我们可以接受某个请求暂时读到完整的旧配置，但不能接受它读到一个新旧字段混合这种从来没有正式发布过的配置。

另外，新配置不会读到后就直接生效。我会先检查 JSON 格式、字段范围以及字段之间的关系，校验失败就继续使用上一份有效配置。Watch 如果因为网络问题断开，也会重新建立监听，并使用 Context 管理 goroutine 的退出。

这样就形成了主被动两级降级：对于已经知道的问题，通过配置开关主动跳过；对于运行过程中突然出现的超时或者异常，通过原来的被动降级自动兜底。它们最终都返回标准的默认结果，但主动降级不会发起网络调用，所以返回更快，也不会继续消耗下游资源。

#### Q8. 先说清楚：降级、熔断、限流，三者到底有什么区别？

**你的回答：**

* **限流** ：在入口控制流量，超过阈值就拒绝，本质是"保护自己不被压垮"。
* **熔断** ：当下游持续失败或超时到一定程度，打开开关，后续请求直接快速失败、不再调下游，本质是"保护下游、同时保护自己不空等"，并且有半开状态去试探下游是否恢复。
* **降级** ：服务有损但仍可用，牺牲一部分功能或数据，保证核心能力不中断，比如给个默认值继续返回。

我现在做的是 **降级** ——下游挂了给它构造默认结果，其他数据源继续聚合，整笔请求不失败。

**追问：你现在是"挂了给默认值继续跑"。如果下游不是挂，而是大面积超时把你的连接池或线程打满，降级还扛得住吗？**

**你的回答：**

扛不住，这正好是降级和熔断的分界。我的被动降级虽然兜住了结果，但每次还是会 **真实发起调用** ，然后等超时或重试失败才兜底。如果下游是慢而不是挂，每次请求都会占用连接、线程，拖慢整体耗时，甚至把资源池打满。

这种情况就需要 **熔断** ，也就是引入"关闭—打开—半开"的状态机：失败率或超时率达到阈值就打开，请求直接快速失败，不再真正调下游；过一段时间进入半开，放少量请求试探，成功就关闭恢复。我现在的主动降级开关其实部分承担了"不调下游"的职能，但它是人工/配置驱动的，不是自动基于失败率触发的，这是它和熔断的核心区别。

---

#### Q9. 被动降级的默认值是怎么定的？下游怎么区分"降级默认值"和"真实正常值"？

**你的回答：**

默认值用的是业务上约定的特殊值，语义上表示"该数据源本次未生效"。但光靠约定俗成的 magic number 确实有风险——万一真实分数恰好等于这个值，就会被误判成降级。

所以更稳妥的做法是 **在响应里加一个显式的降级标记字段** ，明确告诉下游"这个字段是降级兜底出来的，不是真实查询结果"，而不是只靠魔法值猜。魔法值作为内部约定可以存在，但必须配上显式标记，同时把约定文档化，否则时间长了新同事根本不知道这个数字是什么意思。

---

#### Q10. 线上不止一个实例，每个都自己 Watch 配置，会不会出现 A 实例拿到新配置、B 还是旧配置？

**你的回答：**

会，这是分布式配置推送的固有问题，存在一个短暂的"不一致窗口"。运维在配置中心改了开关，A 实例先收到、B 实例后收到，同一段时间内两个实例行为不一致。

我的处理是： **接受这种短暂不一致，但保证单实例内部一致** 。降级开关本质是"尽力止损"，不是强一致要求，几秒钟的推送延迟在业务上可以接受。真正要避免的是单实例内部读到"一半新一半旧"，所以我才用 `atomic.Value` 一次性整体替换，而不是逐字段改。

如果以后对一致性要求更高，可以做配置 **灰度发布** ——先推到部分实例观察，确认没问题再全量，这样把不一致的爆炸半径控制住。

**追问：同一个用户两个请求打到不同实例，行为不一致怎么办？**

**你的回答：**

短期接受。因为这个场景里不一致的代价是"某笔请求多调了一次下游"或者"某笔请求提前走了降级"，结果都是可容忍的，不会造成数据错误，最终会收敛一致。真正要规避的是"配置语义本身错了"这种情况，那才是全局性事故，靠的是下一题的校验和回滚，而不是强一致推送。

---

#### Q11. 如果新配置 JSON 合法、字段范围也对，但语义错了（比如超时配成 0），你的校验能拦住吗？拦不住怎么回滚？

**你的回答：**

拦不住，这是配置校验的天花板——校验只能保证"格式合法、范围合法、字段关系合法"，但拦不住"合法但错误"的值，比如超时配成 0 这种在语法上完全没问题、业务上却致命的配置。

所以真正兜底的是 **版本管理和快速回滚** 。配置中心要保留历史版本，一旦发现新配置语义错误，能一键回退到上一个已验证版本，而不是靠人工手忙脚乱再改一遍。校验是第一道防线，版本回滚是第二道防线，两者缺一不可。

---

#### Q12. 你既调下游、又做监控、又做降级，如果这些都失败了，怎么保证"风控主链路"优先？

**你的回答：**

一句话总结：**分清强依赖和弱依赖，旁路逻辑绝不阻塞主链路。**

* **强依赖** ：下游数据源查询 + 规则聚合 + 返回结果。这部分必须成功，或者通过降级兜底保证不失败。
* **弱依赖** ：监控上报、审计记录这类旁路能力。失败就失败，只影响观测，不影响这笔请求的结果。

所以我所有的旁路逻辑（监控打点、上报）都放在请求主流程之外或统一收口处，用 [`defer`](command:gongfeng.gongfeng-copilot.chat.open-symbol-in-file?%5B%7B%22%24mid%22%3A1%2C%22fsPath%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22external%22%3A%22file%3A%2F%2F%2FUsers%2Fzhouzehua%2Frating%2F%25E5%25AE%259E%25E4%25B9%25A0%25E7%25BB%258F%25E5%258E%2586.md%22%2C%22path%22%3A%22%2FUsers%2Fzhouzehua%2Frating%2F%E5%AE%9E%E4%B9%A0%E7%BB%8F%E5%8E%86.md%22%2C%22scheme%22%3A%22file%22%7D%2C%22defer%22%2C%5B%7B%22line%22%3A50%2C%22character%22%3A13%7D%2C%7B%22line%22%3A50%2C%22character%22%3A18%7D%5D%5D) 或异步处理，保证它挂了不会反过来把风控接口拖垮。这是整个设计里最核心的一条原则：**主业务优先，观测和容灾都是服务主业务的，不能喧宾夺主。**

# 快手实习经历

### 1.项目介绍：

KRP2 是公司内部面向推荐业务的 **引擎服务管控平台** ，

属于云原生体系中的产品管控层。它本身不直接调度服务器资源，

而是在底层容器调度平台之上封装了一层统一的服务管理能力，

再由调度平台调度公司内部的 CPU、GPU、内存等资源池。

平台主要负责引擎服务的全生命周期管理，

包括服务创建、环境配置、部署升级、灰度发布、扩缩容、重启、下线、监控告警和操作审计等。

用户只需要在平台上提交操作，平台会进行参数校验、权限控制和流程编排，

再将任务下发到底层调度系统执行，同时持续同步执行进度和实例状态。

部署、扩缩容和下线等耗时操作通过**工作流和任务 Runner** 异步执行，

并统一对接不同的容器调度系统，从而屏蔽底层平台在接口和资源模型上的差异。

平台还集成了资源配额、配置中心、服务发现、监控告警和消息通知等基础设施。

## 2.工作点：

### 工作点一：

业务在线上环境和测试环境使用或者测试开发功能的时候，

会使用一些临时的泳道隔离环境用于流量隔离。这些环境无人使用到期后，

对应的环境资源需要被清理回收，否则会形成幽灵环境，

这会导致管控平台侧数据库的environment表中残留无效记录，

他的下游调度平台侧会有对应的环境节点未释放占用集群配额，

就会继续占用公共资源池的 CPU、内存等资源。针对这个问题，我负责了自动回收机制。

我们的系统通过 Kafka 监听来自上游泳道管理系统发来的环境到期事件信息，

我们订阅这个topic，当上游管理系统的某个泳道状态变为开始销毁的时候，上游会通过binlog给到kafka，

我们管控平台的消费端获取泳道 ID，并查询该泳道关联的所有未删除环境。

因为一个泳道可能关联多个环境，所以一条消息需要批量处理多个关联环境。

因为我们的下游调度平台有两个，一个是容器云平台，一个是基于k8s设计的云平台，

他俩共用底层的一套公共资源池。那么我们通过一个泳道的id关联出一个环境的列表之后，

进行依次判断这个环境是来自于哪一种类型的调度平台，如果是普通的容器云平台，

我们只需要软删除本地数据库表的一些信息，把delete字段标记成true，

容器云平台侧会自己完成实例下线与删除环境。

如果这个环境是来自于k8s平台（由于他没有自动删除的机制），则需要先实时查询 Pod 状态，

如果已经没有实例，就直接调用 SDK 删除k8s平台的这个云端环境，如果有实例，

我们这边管控平台需要开一个异步的工作流先调用SDK去k8s下线这个实例，

再删除他的环境，确认完成后同步软删除管控平台的这个环境。

因为kafka消息可能重复，所以同时加入幂等处理、失败重试、操作审计和结果通知，保证管控平台的数据库与容器平台状态一致。

#### Q1. 什么是泳道：

可以把“泳道”理解成一个跨服务的临时隔离标签，而“环境”是某一个具体服务在这个标签下创建的运行空间。

例如，一次请求需要经过：

```
网关 → 特征服务 → 召回服务 → 排序服务
```

开发者要测试新功能，但又不能影响正常测试环境，于是创建一个名为 `feature-x` 的泳道。这个泳道可能包含：

```
feature-x 泳道
├── 网关服务的 feature-x 环境
├── 特征服务的 feature-x 环境
├── 召回服务的 feature-x 环境
└── 排序服务的 feature-x 环境
```

#### Q2. kafka消息重复消费的原因：

**1.（消费者端）业务成功后提交offset失败可能会出现重复消费：**

Kafka 中每个 Topic 被拆成多个 Partition，每条消息在 Partition 内都有唯一递增的 offset编号，
如果是消费者正常运行的状态，这个时候，消费者poll消息之后会在消费者的本地内存中指针+1，然后处理业务，

处理成功之后会给kafka的broker的__consumer_offsets做一次offset提交，

如果消费者是刚启动的状态，这个时候，他会去broker的__consumer_offsets去查一下offset，接着这个offset的消息消费。

所以：业务执行成功和 offset 提交成功是两个独立操作，中间存在失败窗口。

结合泳道回收场景：业务成功，提交 offset 之前"这个中间点，

进程挂了网络断了，offset 还没来得及提交，

然后kafka这边心跳超时触发了rebanlance，

而第一次消费实际上已经删除了环境，但是 Kafka 不知道。

rebanlance机制重新分配这个 Partition 给一个consumer，

他读取已经提交的 offset，发现消费组只确认到 offset 2，

于是再次读取offset 2 。由此产生重复消费。

我们的项目一次消息会清理同一泳道下的多个环境，并调用外部容器平台，

属于长耗时任务。如果处理时间超过消费者允许的最大处理间隔，就更容易触发 Rebalance，导致消息会重复消费。
**解决方案**：

做消息级幂等键。上游给每条消息生成一个唯一的 `event_id作为幂等键（在我们场景下我使用的是泳道id+事件类型和事件版本拼成一个event_id）`，

消费端维护一张消费记录表。业务全部执行成功后，

先把这个 `event_id` 标记为 `SUCCESS`，然后再提交 offset。

如果 offset 提交失败导致消息重新消费，消费者先查询 `event_id`，

发现这条消息已经成功处理过，就直接跳过整条业务，再次提交 offset

**2.（消费者端）业务失败导致没有提交offset可能出现重复消费：**

假设一个泳道关联三个环境：

```
泳道 1001
├── 环境 A
├── 环境 B
└── 环境 C
```

第一次消费：

```
环境 A：删除成功
环境 B：删除成功
环境 C：删除失败
Kafka 消息：整体没有完成
```

如果不提交 offset，稍后重试时：

```
环境 A：再次处理
环境 B：再次处理
环境 C：再次处理
```

**解决方案：**

针对这种情况，只做消息级幂等是不够的，因为整条消息还没有成功，

不能直接跳过。因此需要把处理状态记录到每个环境上。

重新消费时，先进行消息幂等判断，然后如果幂等键不是sucess，

进入子任务级别的幂等判断：比如一共有ABC三个子任务，A、B 发现已经处理成功就直接跳过，只继续处理失败的 C。

等 A、B、C 全部完成以后，再把整条消息标记为成功，最后提交 offset。

如果某个子任务多次重试仍然失败，就进入重试队列或死信队列，同时告警并通过人工补偿或对账任务处理。

所以第二种情况的核心是：通过子任务级幂等和状态记录，实现断点续做，只重试没有完成的部分。”

**3.（消费端）自动提交 offset 也可能引发重复消费**

如果开启自动提交，消费者会周期性提交当前消费进度。如果业务处理完就宕机了，

但这个时候还没到自动提交的时间点，等到消费者再次重启之后，

会重新拉取broker的offset对应的patition消息进行消费，结果就会重复消费。
**解决方案：**

使用手动提交。因为资源删除这种重要业务，如果 offset 自动提交后，

删除环境时服务宕机或容器平台调用失败，Kafka 会认为消息已消费，重启后不会再处理，导致环境漏删。

**4.（生产端）生产者重试也会产生重复消息：**

例如上游发送泳道销毁消息：

```
Producer                    Kafka Broker
   │                              │
   │── 发送 destroying 消息 ─────>│
   │                              │── 写入成功 offset=2
   │<──── ACK 在网络中丢失 ───────X
   │
   │  Producer 不知道是否写成功
   │
   │── 重试同一条消息 ───────────>│
   │                              │── 再次写入 offset=3
```

Broker 实际收到了两次：

```
offset=2  channel_id=1001 status=destroying
offset=3  channel_id=1001 status=destroying
```

**解决方案：**

Kafka 支持幂等生产者，Broker 会根据`Producer ID + Partition + Sequence Number`

去看两次发送的sequence 是否相同，相同就不会再次写入。

#### Q3. kafka消息丢失的原因：

**1.（消费端）先提交offset后处理业务可能会引发消息丢失**

如果提交完offset之后服务出问题了，还没来得及处理业务，就丢消息了，

比如此时：

* Kafka 认为 offset 2 已经消费；
* 实际泳道环境没有删除；
* 消费者重启后不会重新处理 offset 2。

**2.（消费端）自动提交 offset 也可能引发消息丢失**

比如我消费端一次拉100条消息，刚处理完第1条，自动提交的定时器触发了，

把offset 100提交了——告诉Kafka这100条我都搞定了。结果这时候消费者挂了，

新消费者启动后从101开始消费，剩下99条 **永远不会被处理了** ，因为Kafka认为它们已经消费过了。
解决方法：
这个风险在我们这种长耗时场景下特别明显，所以我们直接关了自动提交，

改成业务处理成功后再手动提交offset，这样就能保证：要么业务成功且offset提交，要么业务失败且消息会重推

反正我们不能接受消息丢失导致环境漏删。

**3.（消费端）批量或并发处理时提交了过大的 offset**

假设一次 poll 到三条消息：

```
offset 0：成功
offset 1：失败
offset 2：成功
```

如果 offset 2 完成后直接提交 3，Kafka 就会认为 0、1、2 都完成了。消费者重启后从 3 开始，失败的 1 就被跳过了。

**解决方法：**

是按 Partition 记录连续完成的位置。即使 2 已经完成，只要 1 还没有成功，就不能越过 1 提交。

**4.（broker端）消息没有被副本可靠保存**

如果生产者使用较弱的确认策略，消息只写入 Leader 就返回成功，

但还没同步到 Follower，Leader 就宕机了，新 Leader 中可能没有这条消息。

例如：

```
消息写入 Leader
→ Producer 收到成功
→ 消息尚未同步到 Follower
→ Leader 宕机
→ 新 Leader 不包含该消息
```

**解决方法：**

我们会让生产者等待所有同步副本确认后，才认为消息发送成功。

同时 Kafka 会配置多个副本，并要求至少有一定数量的同步副本存活。

如果可用副本数量不足，就让本次发送失败，也不能冒险写入，从而降低 Leader 切换时丢消息的风险。

**5.（broker端）消息过期被清理**

Kafka 消息不是永久保存的。如果消费者长期停机或积压时间超过 Topic 的保留时间，旧消息会被清理。

例如：

```
消费者停机 10 天
Topic 消息只保留 7 天
→ 前 3 天的泳道销毁消息被删除
```

**解决方法：**

我们可以根据业务最长恢复时间设置消息保留周期，同时监控消费者积压量。

如果 Lag 持续增长就及时告警，避免消费者还没恢复，消息就已经被 Kafka 删除。

**6.（生产端）消息发送失败，但程序没有正确处理**

例如生产者异步发送消息后，没有等待发送结果，也没有处理失败回调：

```
producer.send(message)
→ 程序认为调用结束就是发送成功
→ 实际发生网络异常或重试耗尽
→ 消息没有写入 Broker
```

**解决方法：**

生产者异步发送消息后，不能调用完就默认发送成功，而是要检查发送回调。

如果发送失败，就进行有限次数重试；多次重试仍然失败，

就记录消息内容并告警，交给后续补偿任务重新发送，不能直接忽略异常。

#### Q4. kafka消息积压的原因：

消息积压本质上就是：生产速度长期大于消费速度.

**1. （消费者端）消费逻辑比较慢**

我们的泳道回收需要查询数据库、调用容器平台，还可能等待实例下线，

这些都是耗时操作。如果单条消息处理时间过长，消费速度就会跟不上生产速度，最终产生积压。

**2. （消费者端）下游系统响应慢或发生故障**

如果容器平台、数据库或者审批系统响应变慢，消费者会一直等待下游返回。

尤其发生超时和重试时，一条消息可能占用很长时间，导致后面的消息不断积压。

**3. （消费者端）消费者实例异常或数量不足**

如果消费者实例宕机、频繁重启，或者消费者数量少于 Partition 数量，

就无法充分并行消费。需要注意，消费者数量超过 Partition 数量也不会继续提升消费能力，多出来的消费者会处于空闲状态。

**4. （消费者端）消费者频繁发生 Rebalance**

如果消费者频繁上下线，或者单次处理时间超过最大处理间隔，

就可能反复触发 Rebalance。Rebalance 期间分区重新分配，消费会暂停，频繁发生时就容易造成积压

**5.（broker端）Partition 数量不足或出现热点 Partition**

如果 Topic 的 Partition 数量太少，消费并发度就上不去。

另外，如果消息 Key 分布不均，大量泳道消息进入同一个 Partition，

就会出现某个 Partition 严重积压、其他 Partition 比较空闲的情况。

**6.（生产者端）上游流量突然增加**

例如平台批量销毁大量泳道，生产者短时间发送大量消息，

而消费者数量和处理能力没有及时扩容，就会出现流量突增导致的积压。

#### Q5. 我们是怎么权衡kafka这些问题的

泳道资源回收不能接受漏处理，否则会造成资源泄漏。因此设计上选择 at-least-once

所以我们的资源回收场景通常选择：

> 先处理业务，成功后提交 offset，接受可能重复，再通过业务幂等解决重复。

消费到泳道销毁消息后，会根据 `channel_id` 查询该泳道下所有 `deleted=0` 的环境，逐个执行回收。环境清理成功后，会将本地环境记录软删除，也就是把 `deleted` 更新为 1。
如果 Kafka 消息重复消费，已经软删除的环境不会再次被查询出来，直接跳过；如果批量清理时只有部分环境成功，重试时也只会处理剩余 `deleted=0` 的环境。因此，同一条消息消费多次，最终环境清理结果与消费一次相同。

操作审计与结果通知：

环境回收属于系统自动发起的删除操作，因此我补充了操作审计，

记录服务、环境、泳道、调度平台、操作时间和清理原因，

支持在操作记录页面查询，便于出现误删或数据不一致时追溯。

清理成功后还会通过即时消息接口发送结果通知操作人。

### 工作点二：

我做的是给共享容器资源池加“配额锁”。因为有些服务在缩容、下线以后，资源虽然空出来了，

但这时候其实不一定能马上放给别人用，万一后面要回滚或者临时恢复，

就会被别的业务抢走，比较被动。所以我加了一个能力，就是在这些释放资源的场景里，

可以把这部分资源先锁一段时间。锁的时候还能配白名单，只有指定的人能继续操作或者使用这个配额，

也能设置锁多久，时间到了自动放开，或者人工提前释放。
为了让这个过程可追踪，我加入了锁定记录查询和配额账单，锁定记录页面可以支持白名单的人主动释放锁资源，

配额变化还会异步生成配额账单，每次资源变化都会异步记一笔账，

前台只负责发起，后端流程里写入和更新账单，

这样后面可以按业务组查历史，也能保证资源状态和账单状态是一致的。

### 工作点三：

我当时做的是服务**变更操作卡单后的状态修复** ，

我们平台会接服务升级、重启、下线、删环境这些变更，

但这些操作不是只改本地一边，而是要同时影响本地数据库和容器云。

问题是这两边不是一个事务，中间只要审批回调没回来、云侧执行报错，

或者本地和云侧更新顺序不一致，就很容易卡单。本地可能还显示在变更中，

但云上其实已经处理完了，或者反过来，后面再提单就会冲突。

我做的事情，就是处理这种卡单问题的 数据**一致性修复** 。

我是先做了一个前置拦截，在变更操作前先检查有没有别的单子在跑，防止它继续叠单。

比如删除环境操作里，我会先查云上服务到底还在不在，

不在了就把本地的 `deleted`、`delete_time` 这些字段补上。

升级、重启这类场景也是一样，云上是运行、暂停还是已经完成，

我就把本地对应的状态同步，尽量以云上的真实结果为准。

然后我做了执行中的补偿，有回滚和兜底策略，

**如果在提单之后的异步工作流过程中容器云那边先失败** ，比如单子还没真正跑完，

我会先把云侧动作撤掉，能取消就取消，能中止就中止，

然后再把本地状态改成取消，避免留下半成品。

**如果云侧已经变更完了，但本地异步工作流的后置步骤失败** ，我不会硬把云侧倒回去，

而是靠后面的同步任务重试，把本地追上云侧；但是某些业务上确实需要反向修正，

这时候就再走回滚单，把云侧真正回滚回来。

审批单如果还挂着，我也会通过审批回调和取消接口，避免一直卡在中间态。

有时候同一个单子会被不同的修复任务反复处理。
比如前一次看到的是“还没删”，后一次其实云上已经删完了。
这时候不能谁最后写库谁说了算，所以我更新本地时会带一个版本校验的并发保护，

意思是“只有你看到的还是老版本，才能改”。
这样就能避免旧结果把新结果覆盖掉。

所以我主要做的是变更卡单后的状态修复和一致性对账，

先防止继续叠单，再按云侧真实状态修本地，

出问题就取消、补偿、回滚，最后把审批和记录一起收口。

### 工作点四：

我们这个平台是分层的，最上面是一个服务，

下面会拆成多个子服务；每个子服务又能创建多个环境，

每个环境下面再跑多个实例。排障的时候，

大家最常看的其实不是“这个服务整体怎么样”，

而是“某个环境现在是不是正常、监控曲线怎么样”。

所以我做的这个功能，就是在服务详情页的环境列表里，

给每个环境补一个直接跳监控系统的入口，点一下就能跳到对应环境的页面。

实现上，后端在返回环境列表的时候，顺手把跳转链接拼好。

这个链接不是固定死的，而是会带上当前环境的信息，

比如地域、机房、阶段、服务名这些，

这样跳过去就是这个环境对应的监控页，不用前端再自己拼，

也能减少点错的情况。我们还做了海内外区分，国内业务走国内域名，

海外业务走海外域名，保证不同地域跳到对应的平台。

另外有个边界场景是，平时一个环境会有自己的“注册名”，

比如这个环境对外提供服务时，在服务注册平台里叫什么名字。

监控系统通常就是靠这个名字去筛选数据的。

但服务下线以后，环境可能还在本地库里能看到，

或者历史记录还需要查，但是这个环境对应的注册名可能已经被清空了。

这个时候如果跳转链接只依赖“环境注册名”，链接就拼不出来，

按钮点了也没用。所以我这里加了兜底逻辑，

优先用环境自己的注册名，如果没有，就退回到云上的服务名继续拼，

保证服务下线后这个入口还是可用的

### 工作点五：

这个工作是给服务创建入口做迁移拦截。

平台里老的容器云服务和新的 KubeRec 服务都能接创建请求，

但为了推动国内业务迁移，我们需要在后端把规则收紧：

国内业务里，凡是已经支持 KubeRec 的服务类型，

就不允许再走容器云创建，必须走 KubeRec。

迁移策略才能真正落地。我当时做的是：

在创建服务和创建子服务这两个创建入口里，

先判断业务是不是海外。海外先保留兼容；如果是国内，

再看这个服务类型是不是已经被纳入“必须走 KubeRec”的那批白名单里。

只要命中这批服务类型，但请求里还想走容器云，就后端直接返回错误，

提示必须用 KubeRec 创建。这样不是只拦前端按钮，

而是把规则放在后端，所以外部 API 直接调用也绕不过去。

## 3.为什么要做前置拦截

因为有些问题一旦放过去，后面补救成本很高，比如叠单、错平台创建、资源被别的业务抢走。前置拦截就是把问题挡在入口，减少后续兜底的复杂度。

## 4.为什么不用分布式事务

因为这几个系统跨得太散，代价高，落地也不现实。像变更、审批、云侧执行这种流程，本身就更适合拆成多个步骤处理，再靠补偿和对账兜底。

## 5.先看云上真实结果，再改本地是什么思路

就是对账修复。因为云侧是实际执行方，本地只是记录方，所以卡单时不能只信本地，要先查云上到底做没做完，再把本地补成一致状态。

## 6.你怎么做异常兜底和排障？

我会把审批记录、操作记录、账单记录都留好，这样后面能查出卡在创建、执行、回滚还是审批。排障时不只是看结果，而是能定位是哪一步出了问题。

## 7.你这个问题本质上是强一致还是最终一致

更偏最终一致。因为本地库、容器云、审批系统不是一个事务里，没法保证一步原子成功，所以我做的是前置拦截、失败补偿、事后对账，最后让状态尽量收敛一致。

## 8.为什么不能直接用事务解决

因为本地数据库、容器云、审批系统根本不是一个事务域。它们之间是跨系统、跨接口、跨流程的，没法像本地数据库那样一把 commit。强行做分布式事务，成本高、耦合重，也不太符合这种长流程操作。

我走的是最终一致性思路，分成三步，先前置拦截避免继续出错，中间失败做补偿，最后按云上的真实结果把本地状态修回来。

# 蔚来实习经历

### 1.项目介绍：

我最近参与的这个项目，是一款 **远程效能与诊断工具软件** 。

在传统的汽车软件开发和测试中，如果测试车跑出了 Bug，

开发人员往往需要抱着电脑，物理连接到车上再去抓取和分析日志，效率极低。
我们这个系统的核心业务场景，就是**帮助研发人员直接‘云端’远程连接目标车辆，**

**一键下发指令抓取车端的海量底层日志（如 DLT、BLF 等），**

**并在后端进行高性能的处理、解析和可视化清洗，**

**帮助开发人员针对性地、快速定位并解决车辆问题。**

这个系统是研发团队保交付、降本增效的核心工具。

系统架构正在从 Python 向 Golang 进行重构，

这样做也是为了支撑日益增长的远程诊断并发请求和海量日志数据，

我主要负责了后端数据从接收、清洗、再传给前端流程的研发与性能优化。

### 2.工作点

工作点一：

我们系统里有一个用来大批量解析dlt 日志的接口，

原来前端发来的每次请求都要花十几秒去完成，体验很不好。

原来的业务流程是，先遍历一遍dlt文件夹里面的所有proto文件，

每当遍历到一个proto文件就会使用正则方式提取我们需要的关键字段，

并生成每个proto文件对应的service对象存在内存中，

这个对象包含了刚才正则提取的字段。第二次 遍历service对象，

每遍历一个对象就会读取一个mapping文件（这个文件是一个解析规则的文件），

然后会按照规则将service对象转化并写入json中，最后把json传给前端。

主管让我去进行优化，我分析后发现，

在第二次遍历service对象的过程中，每次处理service都要读一个mapping文件，

而且我们service对象的数量是很大的，所以磁盘io的次数非常多，

于是我的优化是预读一次mapping规则文件，

把里面的规则内容存在内存的大字典的键值对中，

然后后续遍历处理service只需要走内存就行了，

然后我用brono前端测试发post请求发现从11秒提升到了8秒左右。

第二个优化点是第一次遍历的时候通过正则处理文件的行为是串行的，

于是我使用了并发协程+sync.mutex锁来进行处理，

因为每一个service对象也都是存在了内存的map中，map本身也是不安全的，

他的扩容等机制会出现一些问题，例如并发写的时候可能会造成数据冲突，

所以我这里加了mutex锁，然后协程数量我调整的策略是，

根据用户电脑的内核数量来动态调整，因为协程他本质是用户级线程，

真正做到并行还是要根据内核数量来看，

不然一个内核无论开多少并发总时间是不变的，甚至更久。

工作点二：

我们有一个数据洞察模块，从数据库查出来的数据里经常有 NULL 值，

传到业务层处理时就会报错误，前端也会返回500错误。

这是因为我们这个项目是用go去重构原来的python项目，

在python中，如果数据库是空的，python的结构体字段也会默认是Nan，

但是go中我们的结构体的字段是数值类型的，数据库中如果是空的，

就会报错。一开始我的做法比较简单，就是在db层的结构体里用指针类型来接数据库字段，

空值他会变成nil，对于某些不参与数值计算的接口而言，

就比较适用，只需要在db层改动就可以了，

但是有些接口他还需要再业务层进行计算处理，nil是无法计算的，

于是我当时在业务层加了一个自定义转换函数去外包一下每个变量，

如果是nil，转为0进行处理，这样做的缺点就是，业务层大量外包这种函数，

耦合关系复杂。后来我做了重构，在DB 层就引入 sql.Null 这类可空类型做中间变量，

如果读到 NULL，就直接在这里转换成默认值（比如 0 或空字符串），再往上传。
这样就相当于在数据进入业务之前做了一层清洗，业务层拿到的永远是有效值，

代码也更干净稳定，不会因为脏数据而崩溃，而且做到了数据层和业务层的完全解耦。

工作点三：

我们系统要支持好几种日志文件的解析和展示，其中有一个解析log日志的模块，

需要解析成一个csv和两个txt文件保存在本地并用json的方式返回给前端供前端查询查看，

解析的业务逻辑已经做好了，我是负责后端给前端的接口的实现，

对 CSV 文件，因为主要是按时间查日志，我就只提取时间列做一个轻量索引，

前端可以很快地按时间定位，不用加载全部内容。

对 两种TXT 文件，则会把关键报错信息完整提取出来，一次性用json返回给前端展示。

这样完成了整个的接口开发与对接需求的任务。

工作点四：

我负责开发针对我们日志解析的MCP-server端以及cli工具的开发，

进而开发了主后端agent以及他支持的skills，可以使得用户在客户端调用我们的skills去自动完成日志解析成明文，

然后通过脚本高效提取异常错误信息，最后生成html的日志异常错误的报告。

### 3.为什么要本地化而不是中央服务器？

其实我们当初架构设计时，也考虑过是不是把文件全传到中央服务器去解析。

最终决定做本地化部署（类似边缘计算的模式），主要是出于两点考虑：

**1.规避网络带宽与大文件的传输**：

如果每次都要研发人员把这么大的文件通过网络上传到中央服务器，

光是 **上传的时间可能就要几十分钟** ，更不要提万一网络不稳定断点续传的麻烦。

让数据**‘就近计算’**，在本地直接解析，效率是最高的。

2.**算力成本的分散** ：

日志解析过程中，里面包含了大量的正则匹配和文件 IO，是非常消耗 CPU 的。

如果公司几百上千个开发人员同时在中央服务器上解日志，

服务器的 CPU 极易被打满，公司需要为此付出巨大的机器成本。

而采用本地化部署，本质上是 **白嫖了开发人员本地电脑**

**（比如高配的 Mac/Windows 工作站）的算力** ，大大节省了服务端的开销。

### 4.为什么用go重构python？

**1.分发与部署的问题**

因为Python 是解释型语言，如果要把它做成一个本地工具散发给所有开发人员用，

不管是要求每个人配 Python 环境，还是用 `PyInstaller` 这种工具打包，

都非常痛苦。打出来的包体积巨大、启动慢，

而且经常遇到各种操作系统的动态链接库依赖报错。

PyInstaller是一种**假编译（或者说捆绑式打包）**，

原理不是把代码编译成底层的机器码，而是**把整个 Python 解释器、**

**所有依赖库和我们的脚本当作一个压缩包**捆绑在一起。

用户每次双击运行，它都要在后台先解压出一个临时文件夹，

拉起解释器才能跑代码。这就导致启动非常慢，

而且即便是个小脚本，打出来的包也动辄大几十兆。

我们很多同事是用 Mac 办公，但也有一大批用 Windows 的同事。

在 Python 里，你很难在 Mac 电脑上去打包一个干净的 Windows `.exe` 程序，

而  **Go 是静态编译型语言** ，它可以直接交叉编译（`go build`）出一个

 **没有任何外部依赖的独立二进制文件（Single Binary）** 。

对于工具的分发来说简直是降维打击，

用户拿到压缩包，解压后双击就能直接运行。

并且在我的 Mac 电脑上，只需要一句 `GOOS=windows go build`

就能立刻打出一个 Windows 用户直接能运行的 exe，体验极好。

2.**性能与多核利用率的好处**

日志的大批量解析是典型的  **CPU 密集型任务** 。

Python 有  **GIL（全局解释器锁）** ，它的多线程其实是伪多线程，

同一时间只有一个线程在使用一个内核，所以要实现真正的并行，

需要开多进程，这样的话mapping规则文件就要复制多份，内存开销就很大，

其实，Python 传统上有三种并发方式：**多进程**、多线程、协程（asyncio）,

而这个协程其实还是单核并发机制，受制于GIL(主要为了解决网络 I/O 等待问题),

Python 3.13 的 No-GIL 目前还是实验性质，

生态里大量底层的 C 扩展库（甚至包括一些正则和底层处理库）,

在没有 GIL 保护后，还存在线程安全问题.

而 Go 天生支持高并发,他的GMP调度模型就是可以做到多核利用，

这也是我在优化接口时，**能够直接根据 `runtime.NumCPU()` 动态调整协程数，**

**把用户电脑的算力真正‘榨干’** ，

最终把解析时间从 11 秒压到 8 秒的核心原因，这在 Python 里是很难做到的。

3.**工程的可维护性高**

Python 的动态类型在小脚本时代很爽，但系统一大，

数据库里的脏数据（比如 NULL 或 NaN）传到业务层到处乱跑，

一旦报错连堆栈都很难找。换成 Go 这种强类型语言后，

只要结构体或 DB 层映射没对齐，编译期或反序列化时立马就暴露了，

逼着我们（比如我引入 `sql.Null` 做清洗）把脏数据拦截在最底层，

大大提高了工具运行的稳定性。

### 5.采用的什么协议进行抓包，为什么？

我们采用的是tcp协议，因为这些日志类型包括dlt日志文件

（里面是诸多二进制的proto文件）、

BLF文件（BLF 是 CAN/LIN 总线数据的二进制日志格式，适合高频数据采集）、

pcap文件（PCAP 则是网络通信的原始数据包，常用于以太网通信分析）。

我们后端软件远程抓包时，底层一般是通过 TCP/IP 协议进行通信。

### 6.为什么不用rpc协议，区别？

RPC协议（比如gRPC）本身就是用二进制格式传输数据，

效率高、序列化体积小，非常适合微服务之间的数据通讯。

但我们抓包场景和微服务场景有本质区别：

我们抓取的是完整的日志文件（比如DLT、BLF），

这些文件本身就是二进制，体积大，

通常直接用TCP/IP协议进行文件传输，保证数据完整和可靠。

虽然RPC（如gRPC）底层也是基于HTTP/2和TCP，

传输的是二进制流，但它的设计初衷是为了高效传递结构化的小数据，

比如接口参数、业务响应，适合微服务通信和远程调用。
对于我们抓包这种大文件传输场景，直接用TCP传文件更简单高效，

不需要额外做分片、流式处理。如果用RPC传大文件，

还要自己设计分片、断点续传等机制，反而复杂。

### 7.介绍一下你做的agent

我这个 agent 的背景是这样的：我们原来已经有一套给 Cursor 外部 agent 用的 MCP 能力，

比如 pcap、blf、idl 这些解析模块，Cursor 可以通过本地 MCP server 二进制文件直接调起来完成日志或协议解析。

但这种方式更偏开发侧，前端用户没法直接复用，他们也得去安装cursor去配置json，而且得把这些二进制文件安装到本地。

所以我们要在主后端里做一层 agent，

把原来面向 Cursor 的 MCP 能力往业务系统里收口。

这样前端用户可以直接通过自然语言访问主后端的 skills，

由 agent 的skill负责做意图理解、参数提取和能力编排，

再去调用后端内部的 MCP 模块或工具函数，

最终完成日志解析、基线准备、报表生成这些任务。

就是说，我做这个 agent 的核心目的是把原来只服务 Cursor 的外部工具能力，

统一接入到我们自己的后端系统里，让前端和外部 agent 两种入口的效果保持一致。

### 8.（被问到skills.md相关的可以参考这个）

我这个项目里的 skill 是工程化落地版，不是平台化声明式 skill，

它不是把 **SKILL.md** 直接交给平台在运行时就解释执行了，

他是把 skill 做成了工程里的一个任务工作流。

**所以SKILL.md** 在本项目中是能力说明以及边界定义的文件，真正执行是在 **runner.go** 里完成的。

运行时模型主要看的是代码里的 prompt 和底层工具描述。

就是说，我们这里的 skill 重点是，

把自然语言理解、参数补全、任务编排和底层 MCP 能力调用个给串起来了，

不是做了一个很重的平台化系统。

### 9.skills是怎么工作的，怎么进行的？mcp和skill的关系？

在我的项目里，skill是引用了mcp模块底层，**MCP** 和 **skill** 是分层关系。

**MCP** 更像底层工具能力层，负责具体执行，比如 **pcap**、**blf**、**idl** 解析；

**skill** 是 agent 层里面的，面向用户给的任务的工作流的封装。用户请求先进入 **agent** 接口，

再路由到具体 skill。SKILL.md 是给开发者看的说明文档，

skill/runner.go 里是 skill 的具体执行逻辑。

执行时，skill 会先判断是不是 **chat-only**，

再结合模型做意图提取，把自然语言转换成结构化参数，

然后做规则判断和参数补全，

最后它再去调用底层的 **MCP** 模块或者内部工具函数完成执行，

并把结果写入 session，支持后续追问。所以从我项目的角度看，

**skill** 本质上是在做自然语言理解、任务编排、工具调用和上下文管理，

（而 **MCP** 更偏底层能力提供）。

### 10.logparser的解析skill模块是怎么运作的？

在我这个项目里，**logparser** 这个 skill，是一个“日志诊断任务”的工作流封装。

他把“日志解析、异常提取、模型总结、报告生成”这几个步骤串起来了。

用户请求先进入 **agent** 层的 skill 接口。进入之后，

系统会先判断这次请求是不是 **chat-only**，

也就是用户是不是只是在继续追问，而不是重新发起一次日志分析。

如果只是聊天，就直接走会话回复，不会真的触发解析流程。

如果不是 **chat-only**，就会进入这个 skill 的 **runner函数**。

**runner** 会先做一层意图提取，也就是从用户的自然语言里拿到结构化参数，

比如日志路径、日志类型、用户真正想问的问题。

如果用户显式传了这些参数，就优先用显式参数；如果没传，就从自然语言里补出来。

接下来这个 skill 会真正开始执行分析流程。

它先调用项目里的底层日志解析能力，把原始日志转成结构化 JSON。

然后再从这个 JSON 里提取异常片段，

比如 error、exception、timeout 这类关键信号。拿到这些异常片段之后，

再把用户问题、日志摘要和异常信息一起交给大模型，让模型输出诊断总结，

比如主要异常、可能根因和建议排查步骤。

最后再把这些结果生成成一个 HTML 报告，方便用户查看。

流程结束后，系统还会把这次 skill 的执行结果写进 session，

包括日志路径、解析结果路径、报告路径和摘要。

这样用户后面如果继续追问，就可以基于这次分析结果继续聊。

### 11.rules 是怎么做的？

在我这个项目里，**rules** 不是单独做成规则引擎，

他是分散在 **agent skill**、**MCP** 接口约束和业务代码里一起工作的。
比如第一层是 prompt 规则，主要写在 skill 的 **runner.go** 里。

它会先告诉模型当前是一个意图提取器，只能输出固定 JSON 字段，

这样把用户自然语言先生成结构化参数。然后第二层是代码里的硬规则，

比如怎么判断 **chat-only**、显式参数和模型提取结果谁优先、

没有给版本时是否补默认值、要不要先准备 baseline，

这些都不是靠模型自己决定，而是代码强约束。
第三层是底层能力接口的规则，也就是 **MCP** 或内部模块本身对输入输出的要求。

skill 在上层负责任务编排，真正执行时还是要满足底层接口的参数约束和调用方式。
所以我这个项目里的 rule 实现思路不是“一个中心化规则引擎”，他是多层约束，

最终稳定性主要靠代码兜底。

比如在 **log-error-report** 这个 skill 里，**chat-only** 判断就是代码规则；

日志路径、日志类型和问题提取里有 prompt 规则；

而真正执行日志解析、异常提取和报告生成时，

又要遵守底层函数和模块的输入输出约束。

### 12.如何防范恶意输入或者注入？

我这个项目的安全防护更偏 **工程约束型** ，不是做了一个完整的安全中台，

因为它本身是本地化后端场景，不是公网多租户服务。

我的核心思路是尽量 **不要让大模型直接决定执行什么命令** ，而是把执行边界收死。

第一层是  **skill 和 MCP 的能力边界** 。我暴露的不是一个通用执行器，

而是固定的几个 skill 和固定的几个 tool，比如 **pcap**、**blf**、**idl**、日志分析。

用户就算恶意提示，也只能落到这些受控能力里，不能让模型随便生成 bash 或 Python 去执行。

第二层是  **结构化参数约束** 。在 **runner.go** 里我会先让模型做意图提取，

但要求它只返回固定 JSON 字段，比如文件路径、版本号、输出路径这些，

后面代码再做解析和校验。也就是说，LLM 只负责理解语义，不直接拿它生成的自然语言去执行。

第三层是  **代码里的硬控制** 。像主后端这条链路，我后来专门改成直接调项目内函数，

而不是走 CLI；独立 MCP server 才走 CLI。如果需要起子进程，

也都是 **exec.Command** 这种固定二进制 + 固定参数，不走 shell 拼接，所以能避免典型的命令注入。

第四层是  **输入校验** 。像文件路径、版本号这些，

在 handler 和底层 parser 里都有必填校验、路径存在性检查、部分场景下的绝对路径检查，

不会因为模型说了一段话就直接执行。

如果是公网项目的话，我也了解过。

比如我们可以做一些**权限控制** 。
不是谁都能调用所有 skill，不同用户、不同角色、不同租户，

能用的能力和数据范围都要分开。比如有人只能做日志分析，有人才能跑更高风险的解析能力。

然后可以做**租户隔离和数据隔离** 。
如果是公网服务，就不能像本地项目这样默认信任输入了。每个用户上传的日志、pcap、分析结果、session，都要隔开，不能串数据，也不能因为一个 session key 猜出来就拿到别人的结果。

第三个是 **执行环境隔离** 。
如果底层真的要跑解析程序或者子进程，我不会直接让它跑在主服务进程里，而是会放到单独的沙箱环境里，比如容器、隔离 worker、资源受限的执行环境。这样就算有恶意输入，影响面也不会直接打到主服务。

第四个是 **输入校验和限流** 。
公网下恶意输入会更多，所以除了参数校验，还要防止超大文件、恶意路径、频繁调用、批量刷接口这些问题。比如要做文件大小限制、接口限流、并发控制，还有任务超时。

第五个是 **审计和监控** 。
公网服务一定要能追踪是谁、在什么时间、发了什么请求、调用了哪个 skill、最后执行了什么。这样出了问题才能排查，也方便做风控。

第六个是 **大模型本身的防护** 。
比如 prompt injection、越权调用工具、诱导模型泄露系统信息，这些在公网下会更常见。所以我会把模型能看到的系统提示、能调用的工具、返回给用户的数据都做边界控制，不会让模型自己无限制决定。

### 13.skill后续改进方案

我觉得这个项目在初期这样设计是合理的，

因为它的优点很明显，就是开发快、逻辑稳，而且整体比较好控制。
但如果后面 skill 变多了，问题也会慢慢出来。

比如现在 skill 路由基本还是写死的，用户很多时候得自己选要走哪个 skill；

另外一些规则分散在不同的 **runner** 里，后面 skill 一多，

维护成本会越来越高；再往后如果用户的需求不是单一步骤，

而是要几个 skill 串起来一起做，那现在这套方式也会比较别扭。

所以我也考虑后续去演进，
比如我会先把现有 skill 做得更统一一点，

把每个 skill 的名称、输入输出、执行入口这些都收拢起来，

做成一套统一注册的方式。这样以后新增一个 skill，

就不用到处改路由、改 handler、改很多分散逻辑。

后续的话我也会在 **agent** 前面加一层意图路由。也就是用户先说需求，

系统先判断他更适合走哪个 skill，或者是不是需要几个 skill 配合，

而不是一开始就让用户自己选。
但这个阶段我不会完全交给大模型自由决定，

比如可以明显的场景先用规则判断，

不那么明确的再让模型辅助识别，这样会更稳。

如果后面业务再复杂一些，我会考虑让大模型进一步参与 skill 的选择和任务拆解。
但不能完全放开让模型随便选，也会给它一些边界。

比如只让它在我们定义好的 skill 范围里选，传参也必须符合固定格式，

执行失败时要能退回到保守方案，整个过程也要能记录下来，

方便后面排查问题。也比较灵活吧。

### 13.AI coding经验

“我在实际工作中大量使用AI辅助编程，但不是简单地让AI写代码，

而是把AI当作‘智能助手’和‘代码审查员’来用。我的流程大致是：

1. 先用AI理解上级任务，并和自己的理解做对齐，梳理出清晰的需求和开发步骤。
2. 让AI帮我规划详细的开发流程和每一步的注意事项，自己再和领导确认需求，确保方向正确。
3. 编码时，我会用多个AI智能体协作：一个agent负责搭建代码框架和基础实现，另一个agent负责代码审查和潜在风险提示。
4. 对于涉及终端操作（如rm、数据库等高风险命令），我会让AI先详细说明每一步操作，自己严格把关，确保安全。
5. 业务逻辑和通用代码会用AI生成，数据库和关键操作我会亲自实现和测试。
6. 第一版写完后，自己先测试，再交给AI做多轮审查，互相交叉检查，最后确认无误才提交测试

### 14.openclaw的理解

小龙虾我对他的理解其实就是对比了其他的一些智能体，比如cursor。我先说一下他俩的一些差异。

Cursor 本质上是VS Code 进程里的一个子模块 。它的所有能力，都被限制在 IDE 的沙盒环境里，架构上依赖于编辑器的插件 API。它能做的事，比如补全代码、分析语法树，都离不开编辑器这个宿主。

而小龙虾是一个**独立的系统级进程 。它在架构上是 **顶层设计** ，直接对接操作系统内核，拥有系统级的权限。这意味着它在架构上不依赖任何工具，它自己就是一个 **执行平台** 。

**其次，是能力扩展的架构设计不同。**

Cursor 的能力是功能内嵌** 的。比如代码重构、解释逻辑，这些功能是写死在代码逻辑里的，要加新功能就得改源码。

而小龙虾采用的是松耦合的插件化架构** 。它的核心引擎和具体能力是完全解耦的，所有功能（比如操作文件、执行 Shell、访问数据库）都封装成独立的 `Skill` 插件。只要实现统一接口，就能无限扩展。
