
# 华为 Mate 40 Pro 无线投屏（Windows 版）完整指南

## 准备工作：安装 scrcpy

在 Windows 上打开 PowerShell 或命令提示符，执行：

```powershell
winget install --exact Genymobile.scrcpy
```

安装完成后，**关闭并重新打开** PowerShell 窗口，使环境变量生效。

验证安装：

```powershell
adb version
scrcpy --version
```

---

## 1. 开启手机开发者模式

1. 手机进入「设置 → 关于手机」
2. 连续点击「版本号」7 次，输入锁屏密码
3. 返回「设置 → 系统和更新 → 开发人员选项」
4. 开启 **「USB 调试」**
5. 如果存在，同时开启 **「仅充电模式下允许 ADB 调试」**

---

## 2. 首次 USB 连接与授权

1. 用数据线连接手机与 Windows 电脑
2. 手机 USB 模式选择 **「传输文件」**
3. 手机上弹出授权窗口时，勾选 **「始终允许」**，点击 **「允许」**

在 PowerShell 中确认设备：

```powershell
adb devices -l
```

必须显示：

```text
PQY0220C01045453    device product:NOH-AN00 model:NOH_AN00 device:HWNOH
```

---

## 3. 开启手机热点

1. 手机进入「设置 → 移动网络 → 个人热点」
2. 开启个人热点
3. 让 Windows 电脑连接该热点

在 PowerShell 中查询网关地址：

```powershell
ipconfig
```

找到当前连接的 Wi-Fi 适配器，查看「默认网关」。

**华为手机热点默认网关为：`192.168.43.1`**

---

## 4. 开启无线 ADB（首次设置，需要 USB 连接）

保持 USB 连接，在 PowerShell 中执行：

```powershell
# 清除旧的无线连接记录
adb disconnect

# 开启手机的无线 ADB 服务
adb -s PQY0220C01045453 tcpip 5555

# 连接无线 ADB
adb connect 192.168.43.1:5555

# 确认连接成功
adb devices -l
```

确认出现：

```text
192.168.43.1:5555    device
PQY0220C01045453      offline
```

看到 `192.168.43.1:5555    device` 后，**即可拔掉 USB 数据线**。

---

## 5. 启动投屏

### 使用命令行 scrcpy

```powershell
scrcpy -s 192.168.43.1:5555
```

### 使用 eScrcpy 图形界面（可选）

如果你之后安装了 eScrcpy：

1. 打开 eScrcpy
2. 在设备列表中选择 `192.168.43.1:5555`
3. 点击启动镜像

---

## 6. 下次重新连接（无需 USB）

以后每次使用时，只要：

1. 手机开启个人热点
2. 电脑连接手机热点
3. 在 PowerShell 中执行：

```powershell
adb connect 192.168.43.1:5555
scrcpy -s 192.168.43.1:5555
```

---

## 7. 使用完毕断开连接

```powershell
adb disconnect 192.168.43.1:5555
```

---

## 8. 手机重启后的处理

如果手机重启了，无线 ADB 服务会失效，需要重新用 USB 连接，执行：

```powershell
adb -s PQY0220C01045453 tcpip 5555
adb connect 192.168.43.1:5555
```

然后就可以拔掉 USB，继续无线使用了。

---

## 📋 完整命令速查表

| 场景               | 命令                                                                      |
| ------------------ | ------------------------------------------------------------------------- |
| 首次设置（需 USB） | `adb -s PQY0220C01045453 tcpip 5555`  `adb connect 192.168.43.1:5555` |
| 确认连接           | `adb devices -l`                                                        |
| 启动投屏           | `scrcpy -s 192.168.43.1:5555`                                           |
| 日常无线连接       | `adb connect 192.168.43.1:5555`  `scrcpy -s 192.168.43.1:5555`        |
| 断开连接           | `adb disconnect 192.168.43.1:5555`                                      |
| 手机重启后恢复     | 重新 USB 连接，执行`adb -s PQY0220C01045453 tcpip 5555`                 |

---

## ⚠️ 常见问题与解决方法

| 问题                           | 解决方法                                                        |
| ------------------------------ | --------------------------------------------------------------- |
| `adb` 命令找不到             | 确保已通过`winget` 安装 scrcpy，并重新打开 PowerShell         |
| USB 连接后没有设备             | 换一根支持数据传输的数据线；确保 USB 模式选「传输文件」         |
| 手机显示`unauthorized`       | 关闭再打开「USB 调试」，重新授权                                |
| 无线连接提示`cannot connect` | 确保电脑连的是手机热点；执行`adb disconnect` 清除旧连接后再试 |
| 投屏卡顿                       | 降低分辨率：`scrcpy -s 192.168.43.1:5555 -m 1024`             |
| 手机重启后无法无线连接         | 需要用 USB 重新执行`adb -s PQY0220C01045453 tcpip 5555`       |

---

## 📝 备注

- 本指南中的设备序列号 `PQY0220C01045453` 是你这台华为 Mate 40 Pro 的专属序列号，请直接使用
- 无线 ADB 仅在可信任的个人热点或家庭网络中使用
- 投屏期间请保持手机屏幕解锁状态，或开启「保持唤醒」功能
