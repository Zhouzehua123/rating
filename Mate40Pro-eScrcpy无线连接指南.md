# 华为 Mate 40 Pro 无线连接 eScrcpy

## 1. 开启开发者模式

1. 手机进入「设置 → 关于手机」。
2. 连续点击「版本号」7 次，输入锁屏密码。
3. 进入「设置 → 系统和更新 → 开发人员选项」。
4. 开启「USB 调试」。
5. 如果存在，同时开启「仅充电模式下允许 ADB 调试」。

## 2. 首次通过 USB 授权

1. 用数据线连接手机与 Mac。
2. 手机 USB 模式选择「传输文件」。
3. 在手机弹窗中勾选「始终允许」，点击「允许 USB 调试」。
4. 在 Mac 终端确认设备状态：

```bash
adb devices -l
```

必须显示：

```text
PQY0220C01045453    device
```

## 3. 开启手机热点

1. 手机进入「设置 → 移动网络 → 个人热点」。
2. 开启个人热点。
3. 让 Mac 连接该热点。
4. 查询手机热点地址：

```bash
ipconfig getoption en0 router
```

本机当前热点地址为：

```text
192.168.43.1
```

## 4. 开启无线 ADB

保持 USB 连接，执行：

```bash
adb disconnect
adb -s PQY0220C01045453 tcpip 5555
adb connect 192.168.43.1:5555
adb devices -l
```

确认出现：

```text
192.168.43.1:5555    device
```

确认成功后即可拔掉 USB 数据线。

## 5. 启动投屏

### 使用 eScrcpy

```bash
open -a Escrcpy
```

在设备列表中选择 `192.168.43.1:5555`，启动镜像。

### 使用命令行 scrcpy

```bash
scrcpy -s 192.168.43.1:5555
```

## 6. 下次重新连接

开启手机热点并让 Mac 连上后，执行：

```bash
adb connect 192.168.43.1:5555
scrcpy -s 192.168.43.1:5555
```

如果手机重启后无法连接，需要重新用 USB 连接，并重复第 4 步。

## 7. 使用完毕

```bash
adb disconnect 192.168.43.1:5555
```

仅在可信任的个人热点或家庭网络中使用无线 ADB。
