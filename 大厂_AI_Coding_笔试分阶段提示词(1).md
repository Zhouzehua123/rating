# 大厂 AI Coding 笔试：分阶段提示词整理

> 根据用户提供的 7 张视频截图识别整理。已统一标点、层级与 Markdown 格式，方便直接复制使用；未擅自补写截图中没有出现的提示词。

## 一、截图所展示的流程

| 阶段 | 主题 | 核心目标 | 截图情况 |
|---|---|---|---|
| 第 1 步 | 需求对齐 | 先明确题目要什么，不写代码 | 已提供 |
| 第 2 步 | 架构设计 / 生成 Spec | 先讲清项目怎么跑，再决定怎么改 | 已提供（两张截图内容近似重复） |
| 第 3 步 | 拆分 + 开发 | 将 Spec 拆成可执行 Tickets，再逐个开发 | 已提供 |
| 第 4 步 | Code Review | 先测小块，再测模块，最后做联调 | 已提供 |
| 第 5 步 | 验收测试 | 对照 README 和 Spec 运行测试并定位问题 | 已提供 |
| 第 6 步 | 定点修复 | 先找根因，再做小范围修改 | 已提供 |

> 下文只收录能够从图片中可靠识别的原始提示词。

---

## 第 1 步：需求对齐

**阶段原则：** 先明确题目要什么。本阶段不写代码。

### 完整提示词

```text
请先阅读 README、项目目录、已有测试、入口文件、核心代码。

本阶段不要写代码，只做需求理解。
请持续追问我这个计划、设计或想法，直到我们达成清晰的共同理解。

请把它拆成一棵“决策树”：每个关键决定会引出后续决定。你要按轮次提问。每轮只问当前前置条件已经明确的问题，不要问依赖未决答案的问题。

每个问题请包含：
- 问题编号和简短标题
- 问题正文
- 你的推荐答案
- 为什么你推荐这个答案

你负责查事实。凡是可以通过阅读代码、文件、文档、配置或工具获得的信息，你自己去查，不要问我。

但决定必须由我来做。你不能替我决定，也不能默认我同意。

每轮提问后，等待我的回答。根据我的回答更新决策树，再进入下一轮。

当你认为所有关键分支都已经问完时，请总结我们达成的理解，并问我是否确认。

在我明确确认之前，不要开始写代码、改文件或执行实现。
```

### 这一阶段的产出

- 一棵逐步收敛的需求决策树
- 已确认的需求边界与关键决策
- 尚未确认的问题列表
- 用户明确确认后的共同理解总结

---

## 第 2 步：架构设计 / 生成 Spec

**阶段原则：** 让 AI 先讲清项目怎么跑，再决定怎么改。

### 完整提示词

```text
请先阅读题目说明、README、项目目录、已有测试、入口文件和核心代码。

本阶段不要写代码，只整理一份可执行的开发 spec。

请重点回答：

一、题目目标
用简洁语言说明这个题目最终要实现什么。

二、已知信息
列出 README、代码和测试中已经明确的信息，包括输入输出、业务规则、已有模块、运行方式和测试方式。

三、需求拆解
把题目要求拆成清晰的功能点。
每个功能点都要说明：要做什么、影响哪里、如何判断完成。

四、实现思路
说明你建议怎么改。
包括需要关注的模块、核心流程、数据结构、接口关系和边界情况。
不要开始写代码。

五、测试与验收
说明应该如何验证完成。
优先使用项目已有测试方式。
列出需要运行的测试命令、关键测试场景和验收标准。

六、不做什么
明确哪些内容不要做，避免超范围实现。

七、风险点
列出最容易出错的地方，比如边界条件、兼容性、隐藏测试、异常输入或项目约定。

输出后请等待我确认，再进入实现阶段。
```

### 建议的 Spec 结构

1. 题目目标
2. 已知信息
3. 需求拆解
4. 实现思路
5. 测试与验收
6. 不做什么
7. 风险点

---

## 第 3 步：拆分 + 开发

**阶段原则：** 让 AI 先讲清项目怎么跑，再决定怎么改。

### 把 Spec 拆成 Tickets 的提示词

```text
【把 spec 拆成 tickets 提示词】

请根据这份 spec 拆成一组可执行的开发 tickets。不要重新讨论需求，不要修改 spec，不要开始写代码。

要求：
- 每个 ticket 要足够小，适合一个 AI Coding 会话完成。
- 每个 ticket 都要有明确边界、验收标准和测试要求。
- 按依赖顺序排列：基础 → 核心 → 边界 → 收尾。
- 测试跟对应实现放在同一个 ticket 里。
- 每个 ticket 都要对应 spec 的具体部分，并遵守“不做什么”。
- 请先简要说明拆分思路、执行顺序和最大风险点。

然后按下面格式输出：

Ticket <编号>：<标题>

目标
这个 ticket 要完成什么。

背景
它来自 spec 的哪部分，为什么需要做。

修改范围
预计影响哪些模块、页面、接口、状态、数据结构或测试。

任务
列出具体要做的事。

验收标准
完成后应该满足什么，必须可检查。

测试要求
需要增加或修改哪些测试。只测试外部行为，不测试实现细节。

依赖
依赖哪些 ticket。没有就写“无”。

不包含
这个 ticket 明确不做什么。

如果发现 spec 有歧义，不要自行补完。请单独列出“需要确认的问题”，但仍然输出当前能确定的 tickets。

请实现 Ticket <编号>。
```

### Ticket 模板

```markdown
## Ticket <编号>：<标题>

### 目标

这个 ticket 要完成什么。

### 背景

它来自 spec 的哪部分，为什么需要做。

### 修改范围

预计影响哪些模块、页面、接口、状态、数据结构或测试。

### 任务

列出具体要做的事。

### 验收标准

完成后应该满足什么，必须可检查。

### 测试要求

需要增加或修改哪些测试。只测试外部行为，不测试实现细节。

### 依赖

依赖哪些 ticket。没有就写“无”。

### 不包含

这个 ticket 明确不做什么。
```

### 这一阶段的产出

- 按依赖顺序排列的一组小型 Tickets
- 每个 Ticket 的边界、验收标准和测试要求
- Tickets 与 Spec 具体部分之间的对应关系
- 需要人工确认的 Spec 歧义
- 可逐个交给 AI Coding 会话执行的开发任务

---

## 第 4 步：Code Review

**阶段原则：** 先测小块，再测模块，最后做联调。

### 完整提示词

```text
请对刚才实现的改动做 code review。重点检查：

1. 是否符合对应 ticket 和 spec
2. 是否有漏实现或超范围实现
3. 是否有 bug、边界问题或回归风险
4. 测试是否覆盖关键行为
5. 是否符合项目已有代码风格和架构模式

请先列出问题，按严重程度排序，并给出文件位置和修改建议。没有问题也请明确说明。
```

### Review 检查清单

- 与 ticket、spec 的一致性
- 是否漏做或多做
- Bug、边界问题与回归风险
- 关键行为的测试覆盖
- 项目代码风格与既有架构模式
- 问题严重程度、文件位置及修改建议

---

## 第 5 步：验收测试

**阶段原则：** 对照 README 和 Spec，先给结论，不急着改。

### 截图中的操作指令

```text
跑测试集。
根据测试的结果定位问题。
```

### 执行顺序

1. 对照 README 和 Spec 明确验收口径。
2. 运行完整测试集。
3. 根据真实测试结果定位问题。
4. 先汇报验收结论与问题位置，不立即修改代码。
5. 若存在问题，将测试结果交给下一步“定点修复”处理。

> 截图在本步骤中只展示了上述两条简短操作指令，其余内容为对画面标题和阶段原则的结构化整理。

---

## 第 6 步：定点修复

**阶段原则：** 通过率低时，先找根因，再做小范围修改。

### 完整提示词

```text
现在只修一个具体问题。
先定位根因，再做最小修复。

请先输出失败卡片：问题现象、错误类型、影响需求、已知证据、合理推断、未知信息。

然后列出最多 3 个根因假设，每个假设都要写清楚支持证据和验证方式。

本轮只选择一个最可能、影响面最小的假设来修。

修复前，优先写一个能复现问题的测试、脚本或手工验证步骤。

修复时只改必要代码，不改整体架构，不加无关功能。

改完后给我看测试命令。

在我贴回运行结果前，不要说已修复，只能说待验证。
```

### 失败卡片模板

```markdown
## 失败卡片

- 问题现象：
- 错误类型：
- 影响需求：
- 已知证据：
- 合理推断：
- 未知信息：

## 根因假设

### 假设 1
- 支持证据：
- 验证方式：

### 假设 2
- 支持证据：
- 验证方式：

### 假设 3
- 支持证据：
- 验证方式：

## 本轮选择

- 选择的假设：
- 选择理由：
- 最小修改范围：
- 复现或验证步骤：
- 测试命令：
- 当前状态：待验证
```

---

## 二、整套方法的核心纪律

1. **需求没有对齐，不写代码。**
2. **Spec 没有确认，不进入实现。**
3. **事实由 AI 自己查，产品决策由人确认。**
4. **按前置依赖逐轮提问，不一次抛出一堆无序问题。**
5. **实现必须围绕 ticket 和 spec，不漏做，也不超范围。**
6. **测试先覆盖小块，再覆盖模块，最后做联调。**
7. **修复失败用例时，一轮只验证一个最可能的根因。**
8. **优先最小修复，不趁机重构，不添加无关功能。**
9. **没有看到真实测试结果前，只能说“待验证”，不能声称“已修复”。**

## 三、推荐使用方式

1. 将第 1 步提示词交给 AI，完成需求澄清并明确确认。
2. 将第 2 步提示词交给 AI，让其输出可执行 Spec；人工确认后再开发。
3. 使用第 3 步把 Spec 拆成小型 Tickets，按依赖顺序逐个开发并运行相关测试。
4. 实现完成后使用第 4 步提示词做结构化 Code Review。
5. 使用第 5 步对照 README 和 Spec 运行完整测试集，先报告结论并定位问题。
6. 若隐藏测试或验收测试失败，使用第 6 步提示词做单问题、单假设、最小范围修复。
