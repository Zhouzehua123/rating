# 1.Golang内存逃逸现象是什么？

**内存逃逸**是指本应在**栈**上分配的局部变量，因其 **生命周期超出函数作用域** （如被外部引用），而被编译器**转移到堆**上分配的现象。

1. 函数返回**局部变量的指针**
2. 局部变量被**闭包函数引用**
3. 变量大小 **编译时无法确定** （如大对象(切片)、接口动态分发）
4. 变量在函数内部被存入**全局或包级变量**

# 2.三色标记法——混合写屏障机制

![1766229802584](image/golang八股文/1766229802584.png)
